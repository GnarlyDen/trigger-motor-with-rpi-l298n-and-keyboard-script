write this in the terminal and then press enter:
    "py -m serial.tools.list_port"
and, if the raspberry pi is connected, the terminal shouldn't say:
    "no ports found"
instead it should list the port which the pi is connected to (and maybe other ports)